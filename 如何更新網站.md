# 廣利成 SEMICON 網站 — 如何更新

## 網址
- 公司 / 製程頁：https://hsiao12414.github.io/glc-semicon-2026/glc_profile.html
- 電子名片：https://hsiao12414.github.io/glc-semicon-2026/business_card.html
- 攤位全螢幕 QR 展示頁：https://hsiao12414.github.io/glc-semicon-2026/share.html
- GitHub 專案：https://github.com/hsiao12414/glc-semicon-2026

`share.html`：平板／手機打開就是一張大 QR，上方可切「公司・作品 / 加入聯絡人」。
用法：手機瀏覽器開 → 分享 → 加入主畫面，攤位擺著即可讓客人掃。

這個資料夾：`C:\Users\user\Desktop\glc_semicon_site`

---

## 改文字 / 換照片後，怎麼讓網站更新

1. 直接改資料夾裡的檔案：
   - 文字 → 用記事本或 VS Code 開 `glc_profile.html`、`business_card.html` 修改
   - 照片 → 換掉 `img/` 裡的圖（檔名要一樣），或新增後在 HTML 裡改 `<img src="img/xxx.jpg">`
2. 打開「Git Bash」或終端機，切到這個資料夾，執行三行：

   ```bash
   git add -A
   git commit -m "更新展場內容"
   git push
   ```

3. 等 30～60 秒，GitHub Pages 會自動重新發佈，重新整理網頁就看到新版。

> 第一次在新電腦 push 會跳出瀏覽器要你登入 GitHub，登入一次之後就會記住。

---

## 之後想加回產品照（拿到客戶同意後）

1. 把照片放進 `img/`（建議先用 `build_assets.py` 壓縮，或自行縮到長邊約 1600px）。
2. 在 `glc_profile.html` 的 `<div class="gal">...</div>` 區塊，照現有格式加一組：

   ```html
   <figure class="shot"><img src="img/檔名.jpg" alt="說明" loading="lazy">
     <figcaption><div class="cn">中文說明</div><div class="en">English caption</div></figcaption></figure>
   ```
3. 一樣 `git add -A && git commit -m "..." && git push`。

---

## 兩個 QR code 的意義（都在 business_card.html）
- 左：**加入聯絡人 vCard** — 掃描直接存紀曉菁的聯絡資料到手機
- 右：**公司 / 作品頁** — 掃描開啟 `glc_profile.html`

新 QR 圖檔：`img/qr_company_profile.png`（黑閃銀工業風配色 前景 #12203A / 底 #F1EEE4）
產生 QR 的腳本：跟本檔同層的 `build_assets.py`（需要 Python 的 `qrcode[pil]`）

---

## 重新產生 QR / 重新壓縮照片
```bash
python build_assets.py
```
（腳本在 `C:\Users\user\AppData\Local\Temp\...\scratchpad\build_assets.py` 有一份；建議複製到專案資料夾長期保存）
